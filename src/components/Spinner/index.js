import React from 'react';
import './Spinner.css';

const Spinner = () => (
  <div className="app-spinner-container">
    <i className="fas fa-spinner app-spinner" />
  </div>
);

export default Spinner;
